# Here is a list of students and rankings in a list of records. 
# Sort the list called 'records' according to the highest rankings.

records = [["James", 3],
           ["Paul", 4],
           ['Vanessa', 1],
           ['Jules', 2],
           ['Culie', 7],
           ['Frances', 5]]
sorted_data = sorted(records, key=lambda x: x[1])
print(sorted_data)

# Sort the list according to rankings in descending order.
sorted_data.reverse()
print(sorted_data)

